<?xml version="1.0" encoding="utf-8"?>
<!-- Bàn ĐANG CÓ KHÁCH (currentOrderId != null, xem TableDashboardAdapter.bind) — trước đây tô
     nền xanh lá trong suốt (#6677EF1E), nhưng trong ngôn ngữ màu chung của app xanh lá = xanh
     status_completed/accent_ready, tức "hoàn tất/sẵn sàng" — ngược nghĩa với "đang phục vụ".
     Đổi sang tông vàng status_ordering (#FFC107 — đã định nghĩa sẵn cho đúng ngữ cảnh "bàn đang
     có khách/order") để nhất quán: vàng = đang diễn ra, xanh = trống/sẵn sàng. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/chip_pending_bg" />
    <corners android:radius="@dimen/radius_md" />
    <stroke
        android:width="1dp"
        android:color="@color/status_ordering" />
</shape>
